name: Create comment on new issues
on:
  issues:
    type: [opened]
    
jobs:
  comment-with-action:
    runs-on: ubuntu-latest
    steps:
    - name: "github context"
      run: echo '${{ toJSON(github.event) }} |jq
      shell: bash
